model:
  name: Transformer
  max_positions: 5000
  # Transformer setting with 1/4th embedding dimensions than the base setting
  num_blocks:    2
  heads:     8
  emb_dim:     128
  pffn_dim:      512
  drop_prob:     0.3